name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 4 * * 1"
  workflow_dispatch:

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  codeql:
    uses: cplieger/ci/.github/workflows/codeql.yaml@19357decce18d479bf83e80a8d78b67226dfaf24 # v1
