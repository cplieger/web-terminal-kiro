name: CI

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "LICENSE"
      - ".gitignore"
  push:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "LICENSE"
      - ".gitignore"
  workflow_dispatch:

jobs:
  go:
    uses: cplieger/ci/.github/workflows/go-ci.yaml@98c2f685fa19abbbc43735b1108bfd53d30e65e0 # v1
    with:
      app: true
      dockerfile-lint: true
      working-directory: "."
      go-version-file: "go.mod"

  web:
    uses: cplieger/ci/.github/workflows/ts-ci.yaml@98c2f685fa19abbbc43735b1108bfd53d30e65e0 # v1
    with:
      working-directory: "static-src"
      web-lint: true
